version: '3.4'

services:
  cv:
    image: cv
    runtime: nvidia
    stdin_open: true
    tty: true
    volumes:
      - .:/workspace/cv
    build:
      context: .
      network: host
      dockerfile: ./Dockerfile

volumes:
  cv: