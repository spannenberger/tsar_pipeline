version: '3.4'

services:
  cv:
    image: nvidia/cuda:10.2-base
    runtime: nvidia
    stdin_open: true
    tty: true
    volumes:
      - .:/workspace/cv
    build:
      context: .
      network: host
      dockerfile: ./Dockerfile

volumes:
  cv: